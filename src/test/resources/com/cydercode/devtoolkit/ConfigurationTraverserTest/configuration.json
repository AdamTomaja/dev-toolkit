{
  "applications": {
    "Maven": {
      "path": "${maven-path}/mvn"
    }
  },
  "parameters": {
    "distribution": {
      "group": "group1",
      "type": "string",
      "default": "C",
      "values": [
        "A",
        "B",
        "C"
      ]
    },
    "profile": {
      "type": "string",
      "default": "local",
      "values": [
        "dev",
        "local",
        "test"
      ]
    }
  },
  "presets": {
    "build": {
      "application": "Maven",
      "cmd": "clean install -d ${distribution} -p ${profile}"
    },
    "application-error": {
      "group": "group2",
      "application": "dummy-application",
      "cmd": "clean install -d ${distribution} -p ${profile}"
    },
    "clean": {
      "application": "Maven",
      "cmd": "clean"
    }
  }
}